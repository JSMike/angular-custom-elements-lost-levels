# boxes

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build boxes` to build the library.

## Running unit tests

Run `nx test boxes` to execute the unit tests via [Vitest](https://vitest.dev/).
