# Angular Web Component Integration

## Form Associated Custom Elements

* Needs custom CVA, should be built in to Angular
* 

## SSR

## Aliasing / build config (can't use web components directly, need to build)

## IDE Integration

*  can't use typings or web component manifest
*  need to disable html validtion

## Template binding?

* need to test if [binding]="test" syntax binds to attribute only or if it goes to member prop, this may be fine, or may have timing issue
* 
