# Plan

## Objective

Convert the root `README.md` from a scratchpad into a roadmap document and seed the `.issues/` backlog from its existing level 2 headings.

## Steps

- [x] Define a roadmap that reflects the current repo phase and the intended future phases.
- [x] Update `README.md` so each level 2 heading points to a tracked issue.
- [x] Create backlog issue folders for each existing heading.
- [x] Update `.issues/index.md`.
- [x] Record a session summary.

## Notes

- The Angular apps are intentionally not present yet, so the roadmap needs to state that clearly.
- The seeded issues are backlog items, not implementation work.
