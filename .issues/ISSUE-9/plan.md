# Plan

## Objective

Create the first Slidev deck for this repo so the presentation work can evolve alongside the repro implementations.

## Steps

- [x] Create a new issue to track the Slidev deck work.
- [x] Seed `slides.md` with deck headmatter and an initial slide structure.
- [x] Add dedicated slides for the multi-select and calendar-picker repros.
- [x] Build the Slidev deck to verify the Markdown is valid.

## Notes

- The deck should be explicit about which repros are already confirmed versus planned.
- The initial version should stay lightweight and act as a durable scaffold, not a polished final talk.
- The repo also needed `@slidev/theme-default` installed before `slidev build` would succeed.
